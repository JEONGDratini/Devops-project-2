# Use root/example as user/password credentials
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    port: 
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: seay0
      MONGO_INITDB_ROOT_PASSWORD: ocean1220

  fastify: 
    image: seay0/helloworld-was
    restart: always
    port: 
      - '3000:3000'
    environment:
      MONGO_URL: 'mongodb://seay0:ocean1220@localhost:27017'
